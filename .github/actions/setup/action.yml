name: setup
author: kyliann
description: setup
runs:
  using: "composite"
  steps:
    - name: Configure node environment
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: npm
    - name: Install dependencies
      run: npm ci
      shell: bash
